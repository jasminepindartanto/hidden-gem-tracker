---
import { SITE_TITLE } from "@/consts";
import MainLayout from "@/layouts/MainLayout.astro";
---

<MainLayout
  title={`Travel Map | ${SITE_TITLE}`}
  className="w-full p-0"
>
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <link rel="stylesheet" href="https://unpkg.com/leaflet-routing-machine@latest/dist/leaflet-routing-machine.css" />
  
  <div class="px-4 md:px-8 lg:px-12 max-w-7xl mx-auto py-16">
    <div class="mb-12 flex flex-col md:flex-row md:items-end justify-between gap-6">
      <div>
        <h1 class="text-4xl md:text-5xl font-bold text-white mb-4 font-noto-serif tracking-tight">Direction Guide</h1>
        <p class="text-slate-400 font-medium">Temukan rute terbaik menuju Hidden Gem favorit Anda.</p>
      </div>

      <div class="w-full md:w-80">
        <label class="block text-emerald-500 text-sm font-bold mb-2">Pilih Titik Keberangkatan:</label>
        <select id="start-point-select" class="w-full bg-slate-800 border border-slate-700 text-white rounded-xl p-3 outline-none focus:border-emerald-500 transition-all shadow-xl cursor-pointer">
          <option value="gps">📍 Lokasi GPS Saya (Otomatis)</option>
        </select>
      </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-4 gap-8 mb-20">
      <div class="lg:col-span-1 order-2 lg:order-1">
        <h2 class="text-xl font-bold text-white mb-6 flex items-center gap-2">
          <span class="bg-emerald-500 w-2 h-8 rounded-full"></span>
          Destinasi Terdekat
        </h2>

        <!-- FILTER BUTTONS -->
        <div class="flex flex-col gap-4 mb-6">
          <div class="flex gap-2 overflow-x-auto pb-2 no-scrollbar">
              <button data-filter="all" class="filter-btn active bg-emerald-500 text-slate-950 px-4 py-2 rounded-xl text-[10px] font-black uppercase tracking-wider transition-all">Semua</button>
              <button data-filter="hidden" class="filter-btn bg-slate-800 text-slate-400 px-4 py-2 rounded-xl text-[10px] font-black uppercase tracking-wider transition-all hover:text-white border border-slate-700">✨ Hidden</button>
              <button data-filter="non-hidden" class="filter-btn bg-slate-800 text-slate-400 px-4 py-2 rounded-xl text-[10px] font-black uppercase tracking-wider transition-all hover:text-white border border-slate-700">🔥 Populer</button>
          </div>
          
          <!-- TOMBOL KELUAR NAVIGASI -->
          <button id="clear-route-btn" class="hidden w-full bg-red-500/10 hover:bg-red-500 text-red-500 hover:text-white border border-red-500/50 py-3 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all items-center justify-center gap-2 shadow-lg shadow-red-500/10">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
              <line x1="18" y1="6" x2="6" y2="18"></line>
              <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
            Tutup Navigasi
          </button>
        </div>

        <div id="nearest-list" class="space-y-6 max-h-[700px] overflow-y-auto pr-2 custom-scrollbar">
          <p class="text-slate-500 italic text-sm text-center py-10">Menghitung jarak...</p>
        </div>
      </div>

      <div class="lg:col-span-3 order-1 lg:order-2">
        <div id="map" class="w-full h-[500px] md:h-[650px] rounded-[32px] bg-slate-900/50 border border-slate-700 shadow-2xl overflow-hidden relative">
          <div id="loading-state" class="absolute inset-0 z-[1000] bg-slate-900/95 flex items-center justify-center">
            <div class="text-center">
              <div class="w-12 h-12 border-4 border-emerald-500/30 border-t-emerald-500 rounded-full animate-spin mx-auto mb-4"></div>
              <p class="text-slate-400 italic font-medium">Sinkronisasi Database...</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</MainLayout>

<script is:inline src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script is:inline src="https://unpkg.com/leaflet-routing-machine@latest/dist/leaflet-routing-machine.js"></script>

<script is:inline>
  let currentFilter = 'all';
  let allDestinations = [];
  let markerLayerGroup = null;
  let userLatLng = null;
  let routingControl = null;
  let userMarker = null;
  let map = null;

  function calculateDistance(lat1, lon1, lat2, lon2) {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lon2 - lon1) * Math.PI / 180;
    const a = Math.sin(dLat/2) * Math.sin(dLat/2) +
              Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * Math.sin(dLon/2) * Math.sin(dLon/2);
    return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
  }

  const createCustomIcon = (isUser = false, isHidden = false) => {
    const color = isUser ? '#2563eb' : (isHidden ? '#f59e0b' : '#064e3b'); 
    const glowClass = isHidden ? 'glow-active' : '';
    return L.divIcon({
      className: 'custom-div-icon',
      html: `<div class="marker-container ${isUser ? 'user-icon' : ''} ${glowClass}">
              <div class="ping-effect"></div>
              <div class="marker-bounce" style="background: ${color}; border: ${isHidden ? '2px solid #fff' : 'none'}">
                <svg viewBox="0 0 24 24" fill="white" style="width:18px;"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7z"/></svg>
              </div>
            </div>`,
      iconSize: [45, 45], iconAnchor: [22, 22]
    });
  };

  window.clearNavigation = function() {
    if (routingControl) {
      map.removeControl(routingControl);
      routingControl = null;
    }
    const btn = document.getElementById('clear-route-btn');
    btn.classList.add('hidden');
    btn.classList.remove('flex');
    map.closePopup();
  };

  window.getDirections = function(lat, lng) {
    if (!userLatLng) {
      alert("Lokasi Anda belum terdeteksi.");
      return;
    }
    
    window.clearNavigation();

    routingControl = L.Routing.control({
      waypoints: [L.latLng(userLatLng.lat, userLatLng.lng), L.latLng(lat, lng)],
      lineOptions: { styles: [{ color: '#10b981', weight: 6, opacity: 0.8 }] },
      createMarker: () => null,
      addWaypoints: false,
      show: true
    }).addTo(map);

    const btn = document.getElementById('clear-route-btn');
    btn.classList.remove('hidden');
    btn.classList.add('flex');

    routingControl.on('routesfound', function(e) {
      const routes = e.routes;
      const summary = routes[0].summary;
      const timeInMinutes = Math.round(summary.totalTime / 60);
      const distanceInKm = (summary.totalDistance / 1000).toFixed(1);
      const destData = allDestinations.find(d => Math.abs(d.lat - lat) < 0.0001 && Math.abs(d.lng - lng) < 0.0001);

      L.popup()
        .setLatLng([lat, lng])
        .setContent(`
          <div class="p-3 bg-white rounded-xl text-slate-900" style="min-width: 180px;">
            <b class="text-sm block mb-1">${destData ? destData.dtw : 'Tujuan'}</b>
            <div class="bg-emerald-50 p-2 rounded-lg border border-emerald-100">
              <p class="text-[10px] text-emerald-700 font-bold m-0">⏱️ Est: ${timeInMinutes} Menit</p>
              <p class="text-[10px] text-emerald-700 font-bold m-0">📏 Jarak: ${distanceInKm} KM</p>
            </div>
          </div>
        `)
        .openOn(map);
    });
  };

  // FUNGSI UNTUK MENANGANI NAVIGASI OTOMATIS DARI URL
  function handleAutoRoute() {
    const urlParams = new URLSearchParams(window.location.search);
    const lat = parseFloat(urlParams.get('lat'));
    const lng = parseFloat(urlParams.get('lng'));
    
    if (!isNaN(lat) && !isNaN(lng)) {
      // Tunggu sebentar hingga lokasi user terdeteksi
      const checkUserLoc = setInterval(() => {
        if (userLatLng) {
          window.getDirections(lat, lng);
          clearInterval(checkUserLoc);
        }
      }, 500);
      
      // Berhenti mencoba setelah 5 detik jika lokasi tidak ditemukan
      setTimeout(() => clearInterval(checkUserLoc), 5000);
    }
  }

  function refreshDisplay() {
    if (!map) return;
    const currentLat = userLatLng ? userLatLng.lat : -6.9175;
    const currentLng = userLatLng ? userLatLng.lng : 107.6191;

    // Bersihkan rute saat filter diganti agar tidak berantakan
    window.clearNavigation();

    const filtered = allDestinations.filter(d => {
      if (currentFilter === 'all') return true;
      return d.kategori === currentFilter;
    });

    const listContainer = document.getElementById('nearest-list');
    const sorted = filtered.map(d => ({
      ...d,
      distance: calculateDistance(currentLat, currentLng, d.lat, d.lng)
    })).sort((a, b) => a.distance - b.distance);

    listContainer.innerHTML = sorted.length > 0 ? sorted.map((dest, index) => {
      const isHidden = dest.kategori === 'hidden';
      const glowClass = isHidden ? 'glow-card' : 'border-slate-700/50';
      return `
        <div class="bg-slate-800/40 border rounded-2xl overflow-hidden hover:border-emerald-500/50 transition-all group shadow-lg ${glowClass}">
          <div class="relative h-32 w-full overflow-hidden">
            <img src="${dest.photo}" class="w-full h-full object-cover group-hover:scale-110 transition-all duration-500" />
            <div class="absolute top-2 left-2 bg-emerald-500 text-white text-[10px] font-bold px-2 py-1 rounded-lg shadow-lg">#${index + 1} Terdekat</div>
            ${isHidden ? '<div class="absolute top-2 right-2 bg-amber-500 text-white text-[9px] px-2 py-1 rounded-lg shadow-lg animate-pulse font-bold tracking-tighter">✨ HIDDEN GEM</div>' : ''}
            <div class="absolute bottom-2 right-2 bg-black/60 backdrop-blur-md text-white text-[10px] px-2 py-1 rounded-md">★ ${dest.rating}</div>
          </div>
          <div class="p-4">
            <h3 class="text-white font-bold text-sm line-clamp-1 mb-1">${dest.dtw}</h3>
            <p class="text-emerald-400 font-bold text-xs mb-3">${dest.distance.toFixed(1)} km dari lokasi Anda</p>
            <div class="grid grid-cols-2 gap-2">
                <button onclick="getDirections(${dest.lat}, ${dest.lng})" class="bg-emerald-500 hover:bg-emerald-600 text-white py-2 rounded-lg text-[10px] font-bold transition-colors">Lihat Rute</button>
                <a href="/blogs/${dest.id}" class="bg-slate-700 hover:bg-slate-600 text-white py-2 rounded-lg text-[10px] font-bold transition-colors text-center flex items-center justify-center text-decoration-none">Detail</a>
            </div>
          </div>
        </div>
      `;
    }).join('') : '<p class="text-slate-500 text-center py-10 italic text-xs">Tidak ada destinasi di kategori ini.</p>';

    markerLayerGroup.clearLayers();
    filtered.forEach(dest => {
      L.marker([dest.lat, dest.lng], { icon: createCustomIcon(false, dest.kategori === 'hidden') })
        .addTo(markerLayerGroup)
        .bindPopup(`
          <div class="p-0 overflow-hidden rounded-xl bg-white" style="min-width: 220px; color: #1e293b;">
            <img src="${dest.photo}" class="w-full h-28 object-cover" />
            <div class="p-4">
              <div class="flex justify-between items-center mb-1">
                <b style="font-size: 14px;">${dest.dtw}</b>
                <span style="color: #f59e0b; font-weight: bold; font-size: 12px;">★ ${dest.rating}</span>
              </div>
              <p style="font-size: 10px; color: #64748b; margin-bottom: 12px;">📍 ${dest.lokasi || 'Hidden Gem Area'}</p>
              <div class="grid grid-cols-2 gap-2">
                <a href="/blogs/${dest.id}" style="text-align: center; background: #334155; color: white; text-decoration: none; padding: 8px; border-radius: 8px; font-weight: bold; font-size: 10px;">Detail</a>
                <button onclick="window.getDirections(${dest.lat}, ${dest.lng})" style="background: #10b981; color: white; border: none; padding: 8px; border-radius: 8px; font-weight: bold; font-size: 10px; cursor: pointer;">Rute</button>
              </div>
            </div>
          </div>
        `);
    });
  }

  async function initLeafletMap() {
    map = L.map('map').setView([-6.9175, 107.6191], 13);
    // 🔥 WAJIB untuk layout modern (sticky header, flex, grid)
    setTimeout(() => {
      map.invalidateSize();
    }, 300);

    // // 🔥 Observer supaya tidak rusak lagi saat resize / responsive
    // const mapContainer = document.getElementById("map");
    // const resizeObserver = new ResizeObserver(() => {
    //   if (map) map.invalidateSize();
    // });
    // resizeObserver.observe(mapContainer);

    markerLayerGroup = L.layerGroup().addTo(map);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png').addTo(map);

    document.querySelectorAll('.filter-btn').forEach(btn => {
      btn.addEventListener('click', function() {
        document.querySelectorAll('.filter-btn').forEach(b => {
          b.classList.remove('active', 'bg-emerald-500', 'text-slate-950');
          b.classList.add('bg-slate-800', 'text-slate-400');
        });
        this.classList.add('active', 'bg-emerald-500', 'text-slate-950');
        this.classList.remove('bg-slate-800', 'text-slate-400');
        currentFilter = this.dataset.filter;
        refreshDisplay();
      });
    });

    document.getElementById('clear-route-btn').addEventListener('click', window.clearNavigation);

    async function fetchPoints() {
      try {
        const [resStart, resDest] = await Promise.all([
          fetch('/api/titik_awal'),
          fetch('/api/lokasi')
        ]);
        const startPoints = await resStart.json();
        const wisataData = await resDest.json();

        const select = document.getElementById('start-point-select');
        startPoints.forEach(p => {
          const opt = document.createElement('option');
          opt.value = `${p.latitude},${p.longitude}`;
          opt.textContent = p.nama_lokasi;
          select.appendChild(opt);
        });

        allDestinations = wisataData.map(dest => ({
            lat: parseFloat(dest.latitude),
            lng: parseFloat(dest.longitude),
            dtw: dest.dtw,
            id: dest.id,
            kategori: dest.kategori_akses,
            lokasi: dest.lokasi,
            photo: dest.url_foto || 'https://via.placeholder.com/300',
            rating: dest.rating || '0.0'
        })).filter(d => !isNaN(d.lat) && !isNaN(d.lng));

        refreshDisplay();
        
        // Pemicu navigasi otomatis setelah data dimuat
        handleAutoRoute();
        
      } catch (e) { console.error(e); }
    }

    function updateUserMarker(lat, lng) {
      userLatLng = { lat, lng };
      if (userMarker) map.removeLayer(userMarker);
      userMarker = L.marker([lat, lng], { icon: createCustomIcon(true) }).addTo(map);
      map.setView([lat, lng], 14);
      refreshDisplay();
    }

    document.getElementById('start-point-select').addEventListener('change', (e) => {
      if (e.target.value === 'gps') map.locate({setView: true});
      else {
        const [lat, lng] = e.target.value.split(',').map(Number);
        updateUserMarker(lat, lng);
      }
    });

    await fetchPoints();
    map.locate({setView: true, maxZoom: 14});
    map.on('locationfound', (e) => {
      updateUserMarker(e.latlng.lat, e.latlng.lng);
      document.getElementById('loading-state').style.display = 'none';
    });
  }

  document.addEventListener("DOMContentLoaded", () => {
    requestAnimationFrame(() => {
      initLeafletMap();
    });
  });

</script>

<style is:global>
  .leaflet-popup-content-wrapper { padding: 0 !important; overflow: hidden; border-radius: 12px; }
  .leaflet-popup-content { margin: 0 !important; width: 220px !important; }

  .glow-card {
    border: 2px solid #f59e0b !important;
    box-shadow: 0 0 15px rgba(245, 158, 11, 0.4) !important;
    position: relative;
    overflow: hidden;
  }
  .glow-card::after {
    content: ""; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%;
    background: linear-gradient(45deg, transparent, rgba(245, 158, 11, 0.1), transparent);
    transform: rotate(45deg); animation: card-shine 3s infinite;
  }
  @keyframes card-shine { 0% { left: -100%; } 100% { left: 100%; } }

  @keyframes glow-pulse {
    from { box-shadow: 0 0 10px #f59e0b, 0 0 20px rgba(245, 158, 11, 0.4); }
    to { box-shadow: 0 0 25px #f59e0b, 0 0 45px rgba(245, 158, 11, 0.8); }
  }
  .glow-active .marker-bounce {
    animation: bounce 2s infinite ease-in-out, glow-pulse 1.5s infinite alternate !important;
    background: #f59e0b !important;
  }

  .custom-scrollbar::-webkit-scrollbar { width: 4px; }
  .custom-scrollbar::-webkit-scrollbar-thumb { background: #334155; border-radius: 10px; }
  .ping-effect { position: absolute; width: 45px; height: 45px; border-radius: 50%; background: rgba(16, 185, 129, 0.4); animation: ping 1.8s infinite; }
  .marker-bounce { position: relative; width: 32px; height: 32px; border-radius: 50%; border: 3px solid white; display: flex; align-items: center; justify-content: center; box-shadow: 0 4px 8px rgba(0,0,0,0.5); }
  @keyframes ping { 0% { transform: scale(0.1); opacity: 1; } 100% { transform: scale(2.2); opacity: 0; } }
  @keyframes bounce { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-12px); } }
  
  .leaflet-routing-container {
    background: #1e293b !important;
    color: white !important;
    border-radius: 16px !important;
    font-size: 11px !important;
    border: 1px solid #334155 !important;
    padding: 12px !important;
    width: 260px !important;
    max-height: 250px !important;
    overflow-y: auto !important; 
  }

  .leaflet-routing-container::-webkit-scrollbar { width: 4px; }
  .leaflet-routing-container::-webkit-scrollbar-thumb { background: #334155; border-radius: 50px; }
  .leaflet-routing-container::-webkit-scrollbar-track { background: transparent; }
  
  .no-scrollbar::-webkit-scrollbar { display: none; }
  .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
</style>